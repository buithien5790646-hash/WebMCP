# 🧠 Project Context & Memory (AI 交接文档)

> **To the AI Agent**: This file is your **Long-Term Memory**. 
> Read this first to understand the project structure without listing all directories.
> **Update this file** if you make architectural changes.

## 1. Project Overview (WebMCP)

**Goal**: Connect Web-based AI (ChatGPT/DeepSeek) to Local VS Code via MCP Protocol.

### 🏗 Structure
- **Root**: Contains build scripts (`build_release.sh`) and global docs.
- **`/mcp-gateway-vscode`** (VS Code Extension):
  - Acts as the MCP Server (Express).
  - **Port**: Default `34567` (Dynamic range 30000-40000).
  - **Key File**: `src/gateway.ts` (Handles `/bridge` page and WebSocket/HTTP).
  - **Logic**: Generates a secure Token, requires `WebMCP Bridge` to function.
- **`/mcp-bridge-browser`** (Browser Extension):
  - Injects into AI pages to intercept tool calls.
  - **Key Files**:
    - `content.js`: Intercepts JSON tool calls, includes **Periodic Training** (Reminder every 5 calls).
    - `background.js`: Handles communication with VS Code server.
    - `bridge_loader.js`: Content script for the `localhost` handshake page.
    - `popup.html`: Connection status & "Copy System Prompt" button.

## 2. Key Mechanisms (Do Not Break)

### 🔄 Cross-Promotion (互推逻辑)
- **Gateway (VS Code)**: The `/bridge` page (in `gateway.ts`) checks `document.documentElement.getAttribute('data-extension-installed')`. If missing, shows a red warning to install the browser extension.
- **Bridge (Browser)**: The Popup shows distinct guides for "Already Installed" (Start Server) vs "Not Installed" (Go to Marketplace).

### 🧠 AI Memory Maintenance
- **Periodic Training**: `content.js` counts tool calls. Every 5th call, it injects a `system_note` into the JSON response to remind the AI of the JSON format and `list_tools` command.

## 3. Development Norms
- **Language**: TypeScript for VS Code, JS for Browser.
- **Docs**: Bilingual (English Default, Chinese fallback `_zh.md`).
- **Philosophy**: "Digital Witness" - We intentionally keep some legacy comments/files as history.

## 4. Current Status
- Project is **Release Ready**.
- Internationalization (i18n) completed.
- Documentation completed.
